@echo off
rem cccc_env.bat
rem The comment prolog of this file is installed by Inno Setup,
rem the significant lines at the end are appended when make_cccc_env.bat
rem runs as a post installation step.

